{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/main.py",
      "console": "integratedTerminal",
      "justMyCode": true,
      "env": {
        "PROMPT": "test.pyでprintしている文字列をランダムな文字列に書き換える"
      },
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
